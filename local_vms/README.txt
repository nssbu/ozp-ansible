1. Install Vagrant for Windows
2. "cd" into both VM directories and do "vagrant up" and "vagrant provision" for both
3. "cd" into the ubuntu VM directory and do "vagrant ssh"
4. On the ubuntu VM, do "cd /home/vagrant/ozp-ansible" 
   and "source provision_site.sh"